import 'package:flutter/material.dart';
class Resources{
  Resources({
    required this.name,
    required this.description,
    required this.url,
    required this.uploadedBy,
}
      );

  String? name;
  String description;
  String url;
  String uploadedBy;
}
